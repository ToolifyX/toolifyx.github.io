Refactor ALL file-processing tools (image, pdf, batch tools) to use a consistent and correct file handling system.

GOAL

Ensure:
- No mismatch between selected files and processed files
- Clear UX for valid vs invalid files
- Consistent behavior across ALL tools

APPLY TO ALL TOOLS

- Image tools
- PDF tools
- Any batch processing tools

1. FILE CLASSIFICATION (CRITICAL)

When user selects files:

Split into:

const validFiles = []
const invalidFiles = []

Validation rules:
- file size
- total size
- file type

2. DO NOT SILENTLY DROP FILES

- NEVER auto-ignore files
- ALWAYS show rejected files

3. UI FEEDBACK (REQUIRED)

If invalidFiles.length > 0:

Show:

"{validFiles.length} valid • {invalidFiles.length} skipped"

List each invalid file:

- filename
- reason:
  - "Too large"
  - "Unsupported format"
  - "Exceeds total size"

4. BUTTON LABEL (FIX BUG)

Button MUST reflect validFiles only:

- If all valid:
  "Process {validFiles.length} files"

- If some skipped:
  Show warning above
  Button:
  "Process {validFiles.length} files"

5. PROCESSING LOGIC

- ONLY process validFiles
- Use queue processing (NO Promise.all)

for (const file of validFiles) {
  await process(file)
}

6. PROGRESS UI

Replace any:

"Queue (x/y)"

With:

"Processing x of {validFiles.length} files..."

7. RESULT COUNT

Ensure:

results.length === validFiles.length

8. DOWNLOAD ALL (SYNC)

- Only include processed files
- Use results array

9. OPTIONAL AUTO-FIX (BONUS)

Instead of rejecting:

- If file too large:
  → auto resize

- If unsupported format:
  → convert if possible

10. REUSABLE LOGIC

Create:

/lib/fileValidation.ts

Export:

- validateFiles(files, limits)
- returns { validFiles, invalidFiles }

11. CONSISTENCY (IMPORTANT)

Apply SAME logic to:

- ImageUploader
- PDFUploader
- Any batch tool uploader

OUTPUT

Return:

1. fileValidation.ts
2. Updated ImageUploader
3. Updated PDFUploader
4. Example integration in 1 image tool
5. Example integration in 1 PDF tool