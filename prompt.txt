Refactor ALL image tools (Crop, Resize, Editor, Compressor preview) to use a FULL-WORKSPACE EDITOR LAYOUT similar to iLoveIMG.

GOAL

The current UI is too small and form-like.

Upgrade to a professional editor layout where:

- image workspace is LARGE (primary focus)
- controls are secondary sidebar
- user focuses on image, not UI

GLOBAL LAYOUT CHANGE

Replace current layout with:

DESKTOP STRUCTURE:
--------------------
| TOP BAR (tool name + actions)                    |--------------------
| LEFT PANEL |        MAIN CANVAS (LARGE)          |
| (tools)    |        IMAGE WORKSPACE              |
|            |                                     |
|            |                                     |--------------------
| RIGHT PANEL (OPTIONS / PROPERTIES)               |--------------------

MAIN CANVAS (CRITICAL)

- MUST take 70–80% screen width
- min height: 600px+
- centered image
- zoomable workspace
- crop/selection overlay visible clearly

DO NOT shrink canvas inside cards.

LEFT PANEL (TOOLS)

Move editing tools here:

- Crop
- Rotate
- Flip
- Reset
- Mode switch

Make it:

- narrow (200–260px)
- icon + label
- compact spacing

RIGHT PANEL (OPTIONS)

Move settings here:

Crop tool:
- width
- height
- position X/Y
- aspect ratio presets

Resize tool:
- width/height input
- scale %

Keep it secondary UI only.

TOP BAR

Add sticky top bar:

- Tool name
- File name
- Undo / Redo (optional)
- Download button

CROP TOOL SPECIFIC FIX

Fix crop UI:

- crop box MUST be large and draggable
- overlay dim outside crop area
- show handles clearly
- no cramped preview

VISUAL PRIORITY RULE

PRIORITY ORDER:

1. IMAGE CANVAS (MOST IMPORTANT)
2. LEFT TOOL PANEL
3. RIGHT SETTINGS PANEL
4. TOP BAR

SPACING FIX

- remove unnecessary padding around canvas
- reduce empty whitespace
- ensure full screen utilization

RESPONSIVE

MOBILE:
- stack vertically
- canvas first
- tools collapse into drawer

APPLY TO ALL IMAGE TOOLS

- Image Cropper
- Image Editor
- Image Resizer
- Image Compressor preview mode
- Any tool using canvas preview

RESULT SCREEN MUST REMAIN UNCHANGED

Do NOT modify:
- ResultScreen logic
- download flow

ONLY UI layout changes.

OUTPUT

Return:

1. Shared EditorLayout.tsx (new core layout)
2. Updated Crop tool page
3. Updated ImageEditor page
4. Layout integration guide