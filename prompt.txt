Implement Split PDF tool with modular architecture and minimal impact to existing system.

GOAL

Build a Split PDF tool similar to iLovePDF with:

- Range split
- Extract pages
- Fixed split

Ensure:

- no breaking changes
- reuse existing upload + result system
- isolated logic

STRICT RULE (IMPORTANT)

DO NOT modify:

- existing tools
- shared upload logic
- result screen component

ONLY extend via new modules.

FILE STRUCTURE

Create new folder:

/tools/pdf/split/

Files:

- SplitPdfTool.tsx (main page)
- SplitModeTabs.tsx
- RangeEditor.tsx
- ExtractEditor.tsx
- FixedEditor.tsx
- useSplitPdf.ts (logic)
- pdfSplitUtils.ts (pure functions)

STATE DESIGN

const [mode, setMode] = useState<
  "range" | "extract" | "fixed"
>("range")

MODES TO IMPLEMENT

1. RANGE MODE

UI:

- list of ranges
- each range:
  { from: number, to: number }

Actions:

- Add Range
- Remove Range
- Update range values

State:

const [ranges, setRanges] = useState([
  { from: 1, to: 3 }
])

Option:

- mergeRanges: boolean

2. EXTRACT MODE

UI:

Input field:

"1,3,5-8"

State:

const [pageInput, setPageInput]

3. FIXED MODE

UI:

Split every:

[ pagesPerSplit ]

State:

const [pagesPerSplit, setPagesPerSplit]

CORE LOGIC (IMPORTANT)

Create pure functions:

parsePageInput(input: string) => number[]

splitByRanges(pdf, ranges)
splitByPages(pdf, pages)
splitByFixed(pdf, size)

PDF PROCESSING

Use:

pdf-lib (recommended)

FLOW

1. Upload PDF
2. Select mode
3. Configure input
4. Click "Split PDF"
5. Process
6. Show ResultScreen

PROCESSING RULE

- process sequentially
- no Promise.all
- show progress:

"Processing 2 of 5 files..."

RESULT FORMAT

results = [
  { file: Blob, name: "split_1.pdf" }
]

UI REQUIREMENTS

Tabs:

[ Range ] [ Extract ] [ Fixed ]

RANGE UI

Range 1:
[ from ] [ to ]

+ Add Range

☑ Merge all ranges

EXTRACT UI

Pages:
[ 1,3,5-8 ]

FIXED UI

Split every:
[ 2 ] pages

VALIDATION

- from <= to
- page numbers > 0
- pagesPerSplit > 0
- reject invalid input

SUMMARY (IMPORTANT UX)

Show preview:

"This PDF will be split into X files"

INTEGRATION

- Add route: /tools/pdf-split
- Register tool in config
- Use existing Upload component
- Use existing ResultScreen

SAFETY (CRITICAL)

- no global state changes
- no modification to shared hooks
- all logic inside /tools/pdf/split

OUTPUT

Return:

1. all new files
2. integration steps
3. example usage of pdf-lib