Upgrade all IMAGE tools to support multiple file uploads with limits.
GOAL

Allow users to upload multiple images while preventing performance issues.
REQUIREMENTS

1. MULTI FILE INPUT

- Use:
  <input type="file" multiple accept="image/*" />

- Store files in state:
  const [files, setFiles] = useState<File[]>([])
2. LIMIT RULES

Implement:

- Max files: 5
- Max file size: 5MB per file
- Total size limit: 20MB
3. VALIDATION

On file selection:

- Reject files if:
  - too many files
  - file too large
  - unsupported type

- Show error messages:
  - "Max 5 files allowed"
  - "File too large (max 5MB)"
  - "Invalid file type"
4. FILE PREVIEW

- Show list of selected files:
  - file name
  - file size (KB/MB)

- Optional:
  - image thumbnail preview
5. REMOVE FILE

- Add remove button per file
- Allow clearing all files
6. PROCESSING

- Loop through files:

for (const file of files) {
  // process using canvas
}
7. UX

- Show loading state
- Disable button if no file
- Show result per file OR batch result
8. REUSABLE COMPONENT

Create:

/components/ImageUploader.tsx

Props:
- maxFiles
- maxSize
- onChange(files)
9. INTEGRATION

Update all image tools to use ImageUploader
10. ERROR HANDLING

- Do not crash if invalid files
- Always show feedback
OUTPUT

Return:

1. ImageUploader.tsx
2. Example usage in ImageCompressor
3. Updated logic for multi-file processing